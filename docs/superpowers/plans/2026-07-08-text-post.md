# テキスト投稿機能 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 管理画面に「テキスト投稿」ページを追加し、田中（仮）アカウント（他アカウントも選択可）向けに、動画・記事に紐付かないテキストのみの投稿を「今すぐ投稿」または「既存の投稿キューに追加」できるようにする。

**Architecture:** 既存の `Article` テーブル・`content_type` カラムに新値 `"text"` を追加するだけで、新規テーブル・カラムは作らない。`content_type="text"` かつ画像・動画添付なしの `Article` を作ると、既存の `threads_api.post_to_threads()` の「画像0枚→テキストのみ投稿」分岐にそのまま乗る。新規ページから作成した `Article` は既存の投稿キュー画面・スケジューラ・再投稿機能をそのまま利用する。

**Tech Stack:** Python 3.x / Flask / SQLAlchemy (SQLite) / Jinja2 / Bootstrap 5.3（既存スタックのみ、新規ライブラリなし）

## Global Constraints

- コメントは原則書かない（WHYが非自明な場合のみ1行）
- 既存ファイルを編集する（新ファイル作成は最後の手段。今回はテンプレート1つのみ新規作成）
- 必要な変更だけ行う（リファクタリング・クリーンアップは不要）
- Shell: WindowsなのでPowerShell構文（`$env:VAR`、`&&` 不可）。ただし本プランの動作確認コマンドは Git Bash 経由の `curl` を使う
- JSはフォームsubmitよりfetch APIを使う、が既存の `post_now`/`unqueue`/`retry` ボタンはプレーンな `<form method="post">` submitを使っており、今回のテキスト投稿ボタンもこのパターンに揃える（設計書で確定済み）
- 投稿文字数上限は500文字（Threads APIの上限。`summarizer.py` の `THREADS_MAX` と同じ値）
- テキスト投稿は `pending` ステータスを経由しない。作成時点で直接 `queued`
- このプロジェクトに自動テスト基盤（pytest等）は存在しない。各タスクの検証は開発サーバーを起動しての手動確認（`curl` / DB直接確認）で行う

---

## File Structure

- **Modify: `app.py`** — `import uuid` 追加、`TEXT_POST_MAX_CHARS` 定数追加、新規ルート `/text-post`（GET/POST）追加
- **Create: `templates/text_post.html`** — テキスト投稿フォーム画面
- **Modify: `templates/base.html`** — サイドバー・モバイルナビに「テキスト投稿」リンク追加
- **Modify: `templates/queue.html`** — `content_type == 'text'` の場合、タイトルをリンクなしのプレーンテキスト表示にする（2箇所：キュー中カード・投稿済みリスト）

---

### Task 1: `/text-post` ルート（GET/POST）とテンプレートを追加する

**Files:**
- Modify: `app.py:1-15`（import追加）, `app.py:965-979`（新規ルート追加、`schedule()` の直後）
- Create: `templates/text_post.html`

**Interfaces:**
- Consumes: `database.py` の `Article`, `ThreadsAccount`, `Setting`, `db`（`app.py` に既存importあり）。`threads_api.post_to_threads(app, article_id: int, test_mode: bool = False, account_id: int = None) -> tuple[bool, str]`（既存関数、`threads_api.py:291`）
- Produces: Flaskエンドポイント名 `text_post`（`url_for('text_post')` で参照可能）。後続タスク（Task 2: ナビゲーション）はこのエンドポイント名を使う

- [ ] **Step 1: `app.py` の import に `uuid` を追加する**

`app.py:5` の直後（`import secrets` の次）に追加：

```python
import secrets
import uuid
```

- [ ] **Step 2: `/text-post` ルートを追加する**

`app.py` の `schedule()` 関数の終わり（`app.py:976`、`return render_template(...)` の閉じ括弧の行）から、次のコメント区切り（`app.py:979` の `# ── Threads OAuth 認証 ─...`）までの間に、以下を挿入する：

```python
TEXT_POST_MAX_CHARS = 500  # Threads API の投稿文字数上限


@app.route("/text-post", methods=["GET", "POST"])
def text_post():
    accounts = ThreadsAccount.query.filter_by(is_active=True).order_by(ThreadsAccount.id.asc()).all()

    if request.method == "POST":
        body = (request.form.get("body") or "").strip()
        action = request.form.get("action") or ""
        try:
            account_id = int(request.form.get("account_id") or 0)
        except ValueError:
            account_id = 0

        account = ThreadsAccount.query.filter_by(id=account_id, is_active=True).first()

        if not body:
            flash("投稿文を入力してください", "danger")
            return redirect(url_for("text_post"))
        if len(body) > TEXT_POST_MAX_CHARS:
            flash(f"投稿文は{TEXT_POST_MAX_CHARS}文字以内で入力してください", "danger")
            return redirect(url_for("text_post"))
        if not account:
            flash("アカウントを選択してください", "danger")
            return redirect(url_for("text_post"))
        if action not in ("post_now", "queue"):
            flash("不正なリクエストです", "danger")
            return redirect(url_for("text_post"))

        title = body[:30] + ("…" if len(body) > 30 else "")
        article = Article(
            feed_source="テキスト投稿",
            title=title,
            url=f"text-post:{uuid.uuid4().hex}",
            summary=body,
            status="queued",
            content_type="text",
            account_id=account.id,
        )
        db.session.add(article)
        db.session.commit()
        logger.info("テキスト投稿作成: id=%d account_id=%d action=%s", article.id, account.id, action)

        if action == "queue":
            flash("キューに追加しました", "success")
            return redirect(url_for("queue"))

        from threads_api import post_to_threads
        test_mode = Setting.get("test_mode", "true").lower() == "true"
        success, msg = post_to_threads(app, article.id, test_mode=test_mode, account_id=account.id)
        flash(msg, "success" if success else "danger")
        return redirect(url_for("queue"))

    default_account_id = None
    for acc in accounts:
        if acc.account_label == "田中（仮）":
            default_account_id = acc.id
            break
    if default_account_id is None and accounts:
        default_account_id = accounts[0].id

    return render_template(
        "text_post.html",
        accounts=accounts,
        default_account_id=default_account_id,
        max_chars=TEXT_POST_MAX_CHARS,
    )
```

- [ ] **Step 3: `templates/text_post.html` を新規作成する**

```html
{% extends 'base.html' %}
{% block content %}

<h4 class="mb-4 fw-bold">テキスト投稿</h4>

<div class="card" style="max-width:640px">
  <div class="card-header">
    <i class="bi bi-pencil-square me-2"></i>新規テキスト投稿
  </div>
  <div class="card-body">
    <form method="post" id="text-post-form">
      <div class="mb-3">
        <label class="form-label" for="account_id">投稿アカウント</label>
        <select class="form-select" id="account_id" name="account_id">
          {% for acc in accounts %}
          <option value="{{ acc.id }}" {% if acc.id == default_account_id %}selected{% endif %}>{{ acc.account_label }}</option>
          {% endfor %}
        </select>
      </div>

      <div class="mb-2">
        <label class="form-label" for="body">投稿文</label>
        <textarea class="form-control" id="body" name="body" rows="8"
                  oninput="updateTextPostCount()" placeholder="投稿する本文を入力してください"></textarea>
      </div>
      <div class="d-flex justify-content-end mb-3">
        <span id="text-post-count" class="char-count ok">0 / {{ max_chars }} 文字</span>
      </div>

      <div class="d-flex gap-2">
        <button type="submit" name="action" value="post_now" id="btn-post-now"
                class="btn btn-accent" onclick="return confirm('今すぐ投稿しますか？')">
          <i class="bi bi-send-fill"></i> 今すぐ投稿
        </button>
        <button type="submit" name="action" value="queue" id="btn-queue"
                class="btn btn-outline-info">
          <i class="bi bi-collection-fill"></i> キューに追加
        </button>
      </div>
    </form>
  </div>
</div>

{% endblock %}

{% block scripts %}
<script>
const TEXT_POST_MAX_CHARS = {{ max_chars }};

function updateTextPostCount() {
  const len = document.getElementById('body').value.length;
  const countEl = document.getElementById('text-post-count');
  const over = len > TEXT_POST_MAX_CHARS;
  countEl.textContent = len + ' / ' + TEXT_POST_MAX_CHARS + ' 文字';
  countEl.className = 'char-count ' + (over ? 'over' : 'ok');
  document.getElementById('btn-post-now').disabled = over || len === 0;
  document.getElementById('btn-queue').disabled = over || len === 0;
}

updateTextPostCount();
</script>
{% endblock %}
```

- [ ] **Step 4: 開発サーバーを起動する**

Run: `python app.py`（venv有効化後、バックグラウンドで起動。ポート5000）

Expected: コンソールに Flask 起動ログが出て、`http://localhost:5000` で待受を開始する

- [ ] **Step 5: GETでページが表示されることを確認する**

Run: `curl -s -o /dev/null -w "%{http_code}" http://localhost:5000/text-post`

Expected: `200`

- [ ] **Step 6: 「キューに追加」でArticleが作成されることを確認する**

Run:
```bash
curl -s -c /tmp/cw_cookies.txt -b /tmp/cw_cookies.txt -X POST http://localhost:5000/text-post \
  -d "body=これはテスト投稿です" \
  -d "action=queue" \
  -d "account_id=2"
```

Expected: HTTPレスポンスが `/queue` へのリダイレクト（302）。続けて以下でDBを確認：

Run:
```bash
python -c "
import sqlite3
conn = sqlite3.connect('instance/rock_metal.db')
c = conn.cursor()
c.execute(\"SELECT id, title, status, content_type, account_id, feed_source FROM articles ORDER BY id DESC LIMIT 1\")
print(c.fetchone())
"
```

Expected: `(<id>, 'これはテスト投稿です', 'queued', 'text', 2, 'テキスト投稿')`

- [ ] **Step 7: 500文字超・本文空・不正account_idがサーバー側で弾かれることを確認する**

Run:
```bash
curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:5000/text-post -d "body=&action=queue&account_id=2"
curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:5000/text-post -d "body=test&action=queue&account_id=9999"
python -c "print('x' * 501)" > /tmp/longbody.txt
curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:5000/text-post --data-urlencode "body@/tmp/longbody.txt" -d "action=queue&account_id=2"
```

Expected: いずれも `302`（バリデーションエラー時は `/text-post` へリダイレクト。ステータスコード自体はredirectなので200系ではなく302になることを確認し、続けて `articles` テーブルの件数が Step 6 から増えていないことを確認する）

Run:
```bash
python -c "
import sqlite3
conn = sqlite3.connect('instance/rock_metal.db')
c = conn.cursor()
c.execute(\"SELECT COUNT(*) FROM articles WHERE content_type='text'\")
print(c.fetchone())
"
```

Expected: Step 6 で作成した1件のみ（バリデーションエラーのリクエストではレコードが増えていないこと）

- [ ] **Step 8: 開発サーバーを停止する**

Step 4 で起動したプロセスを終了する

- [ ] **Step 9: Commit**

```bash
git add app.py templates/text_post.html
git commit -m "$(cat <<'EOF'
feat: テキスト投稿ページ（/text-post）を追加

田中（仮）アカウント向けに、動画・記事に紐付かないテキストのみの
投稿を作成できるようにする。content_type='text' の Article を
作成し、既存の投稿パイプライン（post_to_threadsのテキスト投稿
分岐）にそのまま乗せる。
EOF
)"
```

---

### Task 2: ナビゲーションに「テキスト投稿」リンクを追加する

**Files:**
- Modify: `templates/base.html:408-433`（モバイルナビバー）, `templates/base.html:461-511`（デスクトップサイドバー）

**Interfaces:**
- Consumes: Task 1 で作成したエンドポイント `text_post`（`url_for('text_post')`）

- [ ] **Step 1: モバイルナビバーにリンクを追加する**

`templates/base.html` の以下のブロック（スケジュールリンクの直後）：

```html
  <a href="{{ url_for('schedule') }}" class="{% if request.endpoint=='schedule' %}active{% endif %}">
    <i class="bi bi-calendar-week-fill"></i> スケジュール
  </a>
  <a href="{{ url_for('comments_page') }}" class="{% if request.endpoint=='comments_page' %}active{% endif %}">
```

を、以下に置き換える：

```html
  <a href="{{ url_for('schedule') }}" class="{% if request.endpoint=='schedule' %}active{% endif %}">
    <i class="bi bi-calendar-week-fill"></i> スケジュール
  </a>
  <a href="{{ url_for('text_post') }}" class="{% if request.endpoint=='text_post' %}active{% endif %}">
    <i class="bi bi-pencil-square"></i> テキスト投稿
  </a>
  <a href="{{ url_for('comments_page') }}" class="{% if request.endpoint=='comments_page' %}active{% endif %}">
```

- [ ] **Step 2: デスクトップサイドバーにリンクを追加する**

`templates/base.html` の以下のブロック（スケジュール `<li>` の直後）：

```html
        <li class="nav-item">
          <a class="nav-link {% if request.endpoint=='schedule' %}active{% endif %}" href="{{ url_for('schedule') }}">
            <i class="bi bi-calendar-week-fill"></i> スケジュール
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link {% if request.endpoint=='comments_page' %}active{% endif %}" href="{{ url_for('comments_page') }}">
```

を、以下に置き換える：

```html
        <li class="nav-item">
          <a class="nav-link {% if request.endpoint=='schedule' %}active{% endif %}" href="{{ url_for('schedule') }}">
            <i class="bi bi-calendar-week-fill"></i> スケジュール
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link {% if request.endpoint=='text_post' %}active{% endif %}" href="{{ url_for('text_post') }}">
            <i class="bi bi-pencil-square"></i> テキスト投稿
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link {% if request.endpoint=='comments_page' %}active{% endif %}" href="{{ url_for('comments_page') }}">
```

- [ ] **Step 3: 開発サーバーを起動し、リンクが機能することを確認する**

Run: `python app.py`（venv有効化後）

Run: `curl -s http://localhost:5000/ | grep -o 'href="/text-post"'`

Expected: `href="/text-post"` が出力される（ダッシュボードのサイドバーにリンクが埋め込まれている）

Run: `curl -s -o /dev/null -w "%{http_code}" http://localhost:5000/text-post`

Expected: `200`

- [ ] **Step 4: 開発サーバーを停止する**

- [ ] **Step 5: Commit**

```bash
git add templates/base.html
git commit -m "$(cat <<'EOF'
feat: サイドバー・モバイルナビに「テキスト投稿」リンクを追加
EOF
)"
```

---

### Task 3: 投稿キュー画面でテキスト投稿のタイトルをリンクなし表示にする

**Files:**
- Modify: `templates/queue.html:44-48`（キュー中カードのタイトル）, `templates/queue.html:200-205`（投稿済みリストのタイトル）

**Interfaces:**
- Consumes: `Article.content_type`（Task 1 で `"text"` の値が入るレコードが作られる）

- [ ] **Step 1: キュー中カードのタイトル表示を条件分岐にする**

`templates/queue.html` の以下（22行目のカード開始直後、44-48行目）：

```html
        <div style="font-size:.88rem;font-weight:600">
          <a href="{{ a.url }}" target="_blank" class="text-decoration-none" style="color:var(--text);font-weight:700">
            {{ a.title[:70] }}{% if a.title|length > 70 %}…{% endif %}
          </a>
        </div>
```

を、以下に置き換える：

```html
        <div style="font-size:.88rem;font-weight:600">
          {% if (a.content_type or 'article') == 'text' %}
          <span style="color:var(--text);font-weight:700">
            {{ a.title[:70] }}{% if a.title|length > 70 %}…{% endif %}
          </span>
          {% else %}
          <a href="{{ a.url }}" target="_blank" class="text-decoration-none" style="color:var(--text);font-weight:700">
            {{ a.title[:70] }}{% if a.title|length > 70 %}…{% endif %}
          </a>
          {% endif %}
        </div>
```

- [ ] **Step 2: 投稿済みリストのタイトル表示を条件分岐にする**

`templates/queue.html` の以下（197行目の投稿済みカード開始直後、200-205行目）：

```html
      <div style="min-width:0;flex:1">
        <span style="font-size:.82rem">
          <a href="{{ a.url }}" target="_blank" class="text-decoration-none" style="color:var(--text-muted)">
            {{ a.title[:65] }}{% if a.title|length > 65 %}…{% endif %}
          </a>
        </span>
```

を、以下に置き換える：

```html
      <div style="min-width:0;flex:1">
        <span style="font-size:.82rem">
          {% if (a.content_type or 'article') == 'text' %}
          <span style="color:var(--text-muted)">{{ a.title[:65] }}{% if a.title|length > 65 %}…{% endif %}</span>
          {% else %}
          <a href="{{ a.url }}" target="_blank" class="text-decoration-none" style="color:var(--text-muted)">
            {{ a.title[:65] }}{% if a.title|length > 65 %}…{% endif %}
          </a>
          {% endif %}
        </span>
```

- [ ] **Step 3: 開発サーバーを起動し、キュー画面がエラーなく表示されることを確認する**

Run: `python app.py`（venv有効化後）

Run: `curl -s -o /dev/null -w "%{http_code}" http://localhost:5000/queue`

Expected: `200`（Jinja構文エラーがあれば500が返るため、200であることでテンプレートの構文的正しさを確認する）

Run: `curl -s http://localhost:5000/queue | grep -c 'text-post:'`

Expected: `0` より大きい数（Task 1 の Step 6 で作成した `url="text-post:..."` のレコードが `<a href>` としてではなく出力されていること自体は確認できるが、`href` に出ていないことは目視確認が必要。次の Step で目視確認する）

- [ ] **Step 4: ブラウザで `http://localhost:5000/queue` を開き、Task 1 で作成したテスト投稿がタイトルのみ（リンクなし・クリックしても遷移しない）で表示されていることを目視確認する**

- [ ] **Step 5: 開発サーバーを停止する**

- [ ] **Step 6: Commit**

```bash
git add templates/queue.html
git commit -m "$(cat <<'EOF'
fix: 投稿キュー画面でテキスト投稿のタイトルをリンクなし表示にする

テキスト投稿のurlは一意制約回避用の合成値（text-post:<uuid>）で
実URLではないため、記事・動画と違いリンク化しない。
EOF
)"
```

---

### Task 4: エンドツーエンドの手動確認

**Files:**
- なし（コード変更なし。Task 1〜3 の統合動作確認のみ）

**Interfaces:**
- Consumes: Task 1（`/text-post` ルート）, Task 2（ナビゲーション）, Task 3（キュー画面表示）

- [ ] **Step 1: 設定画面でテストモードがONになっていることを確認する**

Run: `python -c "
import sqlite3
conn = sqlite3.connect('instance/rock_metal.db')
c = conn.cursor()
c.execute(\"SELECT value FROM settings WHERE key='test_mode'\")
print(c.fetchone())
"`

Expected: `('true',)` または未設定（未設定時のデフォルトは `text_post()` 内で `"true"` 扱い）。`('false',)` の場合は本Task実行前に一時的に `true` に戻す

- [ ] **Step 2: 開発サーバーを起動し、ブラウザで `http://localhost:5000/text-post` を開く**

Run: `python app.py`（venv有効化後）

- [ ] **Step 3: アカウント選択のデフォルトが「田中（仮）」になっていることを目視確認する**

- [ ] **Step 4: 本文に500文字を超えるテキストを貼り付け、両方のボタンが無効化される（クリックできない）ことを目視確認する。文字数カウンターが赤字で `xxx / 500 文字` と表示されることも確認する**

- [ ] **Step 5: 本文を500文字以内の適当なテスト文に修正し、「キューに追加」をクリックする**

Expected: `/queue` にリダイレクトされ、「キューに追加しました」のフラッシュメッセージが表示される。待機中セクションに、入力した本文の冒頭30文字がタイトルとして（リンクなしで）表示される

- [ ] **Step 6: キュー画面で、Step 5 のカードに「日時指定」ボタンで日時を設定し、スケジュールに反映されることを確認する（既存のスケジュール機能がそのまま使えることの確認）**

Run: 画面上で日時を設定 → 保存 → カードに `MM/DD HH:MM JST 予定` バッジが表示されることを目視確認

- [ ] **Step 7: 同じカードの「今すぐ投稿」ボタンをクリックし、テストモードでの投稿成功を確認する**

Expected: 確認ダイアログ→OK後、「テストモード: 投稿シミュレーション成功」のフラッシュメッセージが表示され、投稿済みセクションに移動する。`threads_post_id` が `test_` プレフィックスになっていることをDBで確認：

Run: `python -c "
import sqlite3
conn = sqlite3.connect('instance/rock_metal.db')
c = conn.cursor()
c.execute(\"SELECT id, status, threads_post_id, content_type FROM articles WHERE content_type='text' ORDER BY id DESC LIMIT 1\")
print(c.fetchone())
"`

Expected: `(<id>, 'posted', 'test_<id>', 'text')`

- [ ] **Step 8: `/text-post` に戻り、今度は「今すぐ投稿」ボタンを直接クリックして即時投稿フローも確認する**

Expected: `/queue` にリダイレクトされ、投稿済みセクションに新しいテキスト投稿が表示される

- [ ] **Step 9: Step 7 または Step 8 で作成された投稿済みテキスト投稿の「🔄 再投稿」ボタンをクリックし、キューに戻ることを確認する（既存の再投稿機能がテキスト投稿でも動作することの確認）**

Expected: 待機中セクションに戻り、`status` が `queued` に戻っていることをDBで確認

- [ ] **Step 10: 開発サーバーを停止する**

このTaskはコード変更を伴わないため、コミットは不要（既に Task 1〜3 で必要なコミットは完了している）
